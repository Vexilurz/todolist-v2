import { applicationReducer} from '../../../app/reducer';

describe(
    'applicationReducer', 
    () => {  
     
        it(    
            ``,
            function(){ 
                //let x = MainContainer
                //const wrapper = mount();
                this.timeout(0);
            } 
        );
        
    }
); 