import { toStoreChanges } from "../../../app/utils/toStoreChanges";

describe(
    'toStoreChanges', 
    () => {  
     
        it(    
            ``,
            function(){ 
                this.timeout(0);
                toStoreChanges
            } 
        );
    }
); 