import { detectChanges } from "../../../app/database/detectChanges";


describe(
    'detectChanges', 
    () => {  
     
        it(    
            ``,
            function(){ 
                this.timeout(0);
                detectChanges
            } 
        );
    }
); 
