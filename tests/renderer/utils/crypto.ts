import { encryptDoc, decryptDoc } from "../../../app/utils/crypto/crypto";


describe(
    'crypto', 
    () => {  
     
        it(    
            ``,
            function(){ 
                this.timeout(0);
                encryptDoc
                decryptDoc
            } 
        );
    }
); 