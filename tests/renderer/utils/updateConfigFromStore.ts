import { updateConfigFromStore } from '../../../app/reducer';

describe(
    'updateConfigFromStore', 
    () => {  
     
        it(    
            ``,
            function(){ 
                this.timeout(0);
                updateConfigFromStore
            } 
        );
    }
); 