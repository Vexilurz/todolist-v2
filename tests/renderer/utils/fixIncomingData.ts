import { fixIncomingData } from '../../../app/utils/fixIncomingData';


describe(
    'fixIncomingData', 
    () => {  
     
        it(    
            ``,
            function(){ 
                this.timeout(0);
                fixIncomingData
            } 
        );
    }
); 
